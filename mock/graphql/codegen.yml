overwrite: true
schema: ./lib/schema/**/*.graphql
documents: null
generates:
    #  ./graphql.schema.json:
    #    plugins:
    #      - "introspection"
  lib/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true

