overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  lib/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
        #  ./graphql.schema.json:
        #    plugins:
        #      - "introspection"

