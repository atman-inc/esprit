version: '3'
services:
  app:
    build: .
    volumes:
      - .:/<%= name %>
      - modules_data:/<%= name %>/node_modules
    environment:
      NODE_ENV: development
    ports:
      - "4000:4000"
    command: npm run dev
volumes:
  modules_data: